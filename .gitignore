# Compiled MQL5 binaries — never version these, only source (.mq5/.mq4)
*.ex5
*.ex4

# Logs
*.log

# TRL logger verification: per-run report copies are regenerated by run_trl_batch.sh
/trl_logger/results/*_utf8.htm
/diagnostics/backtests/*

# Local, machine-specific Claude instructions (never shared/committed)
CLAUDE.local.md
